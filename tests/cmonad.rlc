open int.Int   in
open list.List in
open dp.DP     in

let rec length (l : list real)
      : C int =
  match l with [C int]
  | Nil       -> cunit 0
  | Cons x xs -> clet l_xs = length xs in
                 cunit (1 + l_xs)
in
length
