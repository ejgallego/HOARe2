theory AbsDistr

  use import HighOrd
  use import rlc.Distr

  type absdistr 'a

  function fromDistr (x : distr 'a) : absdistr 'a
  function toDistr (m : absdistr 'a) : distr 'a
  function infer (m : 'a) : absdistr ( 'a)
  function ran (m : absdistr ( distr 'a)) : distr ( 'a)

end
